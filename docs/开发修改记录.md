# 开发修改记录（面试追问材料）

> 本项目所有修改统一记录在此，方便复盘与面试追问。
> 基线：以 `代码/` 目录为准（`202325220520+王楷烽/` 为旧副本，不维护）。
> 技术栈：Spring Boot 3.2.5 + Java 21 + MyBatis + MySQL + Vue3；后续接入 Spring AI Alibaba + 通义千问。
>
> 记录格式：【日期】【阶段-序号】【模块】【改动内容】【为什么改】【面试怎么讲 / 追问预案】

---

## P0-安全与工程质量加固（进行中）

### P0-01【2026-08-27】接口越权修复 —— SecurityConfig 全放行

- **模块**：`config/SecurityConfig.java`、`filter/JwtFilter.java`
- **改动**：
  1. 删除 `anyRequest().permitAll()`，改为白名单 + 其余全部 `authenticated()`：
     - 匿名放行：`/api/auth/**`（登录注册）、`GET /api/departments`（首页/注册页部门下拉）、`/api/files/**`（图片直链）、Swagger/Knife4j 文档路径
     - 其余 `/api/**` 一律要求携带有效 JWT
  2. 自定义 401（未登录）/403（无权限）JSON 响应，替代 Spring Security 默认空响应
  3. `JwtFilter`：删除打印 JWT token 明文和参数的系统输出（**日志泄露 token 风险**），改为 SLF4J debug 级别
- **为什么改**：原配置所有接口匿名可访问，JWT 过滤器只解析 token 不拦截任何请求，等于没有鉴权。
- **面试怎么讲**：安全三原则——「默认拒绝、白名单放行、纵深防御」。URL 级粗粒度（必须登录）+ 方法级细粒度（角色）双层防护。
- **追问预案**：
  - Q：为什么用 URL 规则 + AOP 注解两套？A：URL 规则解决"是否登录"，注解解决"谁能调"，职责分离；且部分接口（如 `/admin/jobs/all`）多角色共用，URL 级写死角色会误伤，注解更灵活。
  - Q：401/403 返回格式？A：统一 JSON `{code,message}`，前端 axios 拦截器已按 401 跳登录、403 提示无权限。

### P0-02【2026-08-27】方法级角色鉴权 —— @RequireRole 注解 + AOP

- **模块**：新增 `aspect/RequireRole.java`、`aspect/RoleCheckAspect.java`、`common/AccessDeniedException.java`；修改 `common/GlobalExceptionHandler.java`
- **改动**：
  1. 新增 `@RequireRole({role...})` 方法注解 + AOP 切面：从 JWT 解析当前用户 → 查库校验角色 → 不匹配抛 `AccessDeniedException`（HTTP 200 + code 403，前端统一处理）
  2. 全局异常处理器捕获 `AccessDeniedException` 返回 403
  3. 给全部敏感接口标注角色：

| 接口 | 原状态 | 现要求 |
|---|---|---|
| POST /department/jobs（发布岗位） | 任意登录用户 | 角色 1/2（企业导师/部门管理员） |
| POST /admin/jobs/audit（岗位审批） | **任意登录用户可审批** | 角色 3（超管） |
| POST /admin/jobs/publish（发布操作） | **任意登录用户可调用** | 角色 3 |
| DELETE /department/jobs/{id}（删岗位） | 任意登录用户 | 角色 1/2 |
| POST /student/applications（提交申请） | 任意登录用户 | 角色 0（学生） |
| GET /student/applications/{userId} | **可查任意学生申请** | 仅本人或超管（所有者校验） |
| GET /department/applications（部门申请） | **学生也能看全部申请** | 角色 1/2/3，且 1/2 仅本部门 |
| POST /department/applications/process（审核申请） | **学生也能审核** | 角色 1/2 |
| GET/POST /admin/users*（用户管理） | 任意登录用户 | 角色 3 |
| /api/department/users/*（学生审批） | 任意登录用户 | 角色 2/3 + 部门归属校验 |
| POST/PUT/DELETE /departments（部门管理） | 任意登录用户 | POST/DELETE 角色 3；PUT 角色 2/3 + 只能改本部门 |
| GET /stats/*（统计） | 任意登录用户 | 角色 1/2/3 |
- **为什么改**：原代码权限全靠 Controller 手写且大量遗漏（`/admin/jobs/audit` 无任何校验），存在严重越权（学生可审批岗位、可审核申请）。
- **面试怎么讲**：注解式鉴权让"权限声明"和"业务代码"解耦；配合手写业务级校验（如部门归属）形成纵深防御。
- **追问预案**：
  - Q：为什么不用 Spring Security 的 @PreAuthorize？A：@PreAuthorize 需要开启方法安全且与 Spring Security 表达式耦合；自定义注解 + AOP 更贴合现有代码风格、可统一返回 Result 格式，也便于扩展（如将来加"数据权限"）。
  - Q：切面怎么拿当前用户？A：JWT 过滤器已把 userId 写入 request attribute，切面从 `JwtUtils.getUserIdFromRequest` 取，再查库拿角色（避免信任 token 里的角色声明被篡改——虽然 JWT 有签名，但查库是最稳的）。

### P0-03【2026-08-27】CSV 导出改为带 Token 下载（配套前端）

- **模块**：`frontend/src/components/DepartmentJobs.vue`、`StudentApplications.vue`
- **改动**：`window.open('/api/export/...')`（浏览器直开，不带 Authorization）→ axios 请求 `responseType:'blob'` + 前端 Blob 下载
- **为什么改**：P0-01 后 `/api/export/**` 要求登录，window.open 无 token 会 401；且 token 放 URL（`?token=`）有泄露风险，故改 axios 方案。
- **面试怎么讲**：鉴权收紧后"非 axios 直链"的兼容处理；前端 blob 下载 + 文件名从 Content-Disposition 解析。

### P0-04【2026-08-27】删除明文密码兜底

- **模块**：`service/UserService.java`（checkPassword）
- **改动**：删除"密码长度 <60 直接明文比较"分支，统一 BCrypt 校验
- **为什么改**：明文兜底等于允许弱口令直通，属于高危；数据库种子数据均为 BCrypt 密文，不受影响。
- **追问预案**：Q：老数据有明文密码怎么办？A：上线前用脚本统一把明文迁移成 BCrypt（系统种子数据本身就是 BCrypt，无存量明文）。

### P0-05【2026-08-27】越权漏洞补漏（查他人申请 / 导出他人数据）+ 既有测试修复

- **模块**：`controller/ApplicationController.java`、`controller/ExportController.java`、`controller/DepartmentUserApprovalController.java`、`controller/DepartmentController.java`、`service/JobService.java`、`config/SecurityConfig.java`、`src/test/...`
- **改动**：
  1. `GET /student/applications/{userId}`：只能查本人，超管除外（原来任何登录用户可查任意学生申请）
  2. `GET /export/applications/csv?userId=`：只能导出本人，超管除外（原来可导出任意用户申请）
  3. `GET /department/users/pending`、`POST /department/users/approve`：部门管理员只能操作本部门（原只校验了目标学生部门，未校验操作者）
  4. `PUT /departments/{id}`：部门管理员只能改本部门（原来可改任意部门）
  5. `JobService.getAllPublishedJobsWithRecommendation`：把查询结果复制为 `new ArrayList<>`，避免不可变列表 `sort` 抛 `UnsupportedOperationException`（生产健壮性 + 修复既有单测）
  6. `ApplicationServiceTest.testProcessApplicationApprove`：补漏 stub `jobMapper.selectById`（既有测试缺陷）
  7. `SecurityConfig`：`csrf()`/`sessionManagement()` 链式写法改为 Spring Security 6 lambda DSL，消除编译警告
- **验证**：`mvn test` → **25 个测试全部通过**（Auth 3 / Application 4 / Job 9 / User 9），BUILD SUCCESS
- **面试怎么讲**：除"谁能调接口"外，还要做"谁能看谁的数据"（数据级越权）；修复过程顺带暴露并修复了既有测试缺陷，体现测试素养。
- **追问预案**：
  - Q：为什么 GET /departments 匿名放行？A：首页/注册页需要部门下拉，无需登录；且仅暴露部门名称/描述，无敏感数据。
  - Q：401 和 403 的区别？A：401 未认证（没带或带了无效 token）；403 已认证但角色/数据权限不足。

---

### P0-06【2026-08-27】publishJob 状态 bug 修复

- **模块**：`service/JobService.java`、`src/test/.../JobServiceTest.java`
- **改动**：
  1. `publishJob()`：原逻辑把状态设为 **2（已结束）**，与"发布"语义相反 → 修正为 **1（招聘中）**；增加岗位不存在校验；已是招聘中则幂等返回
  2. 删除 `submitJob`/`auditJob` 中残留的 `System.out.println`（日志规范收尾）
  3. 修正 `JobServiceTest.testPublishJob`：**旧测试断言了错误的 status=2**（bug 因此长期未被发现），改为断言 status=1；新增 `testPublishJobNotFound`
- **为什么改**：状态机语义错误——发布接口会让岗位直接变成"已结束"。
- **面试怎么讲**：状态机 bug 修复 + "测试把错误行为固化为断言"的经典案例：修 bug 时必须同步审视既有断言是否在守护错误行为。
- **追问预案**：Q：岗位状态机完整流转？A：0 待审批 → 1 招聘中（审批通过）→ 2 已结束 / 3 拒绝 / 4 已招满；录用达配额自动置 4；publishJob 负责从非招聘中状态重新发布。

### P0-07【2026-08-27】通知闭环 —— sendNotification 从"写了没人调"到全流程接入

- **模块**：`service/ApplicationService.java`、`service/JobService.java`、`mapper/JobMapper.java`、`mapper/ApplicationMapper.java`
- **改动**（通知类型：1-岗位通知，2-申请通知）：
  1. 学生提交申请 → 通知岗位发布人"收到新申请"（relatedId=申请ID）
  2. 部门审核申请（通过/拒绝）→ 通知学生审核结果 + 审核备注
  3. 超管审批岗位（通过/拒绝）→ 通知发布人审批结果 + 原因
  4. `publishJob` 重新发布 → 通知发布人
  5. 两个 Mapper 的 `insert` 增加 `@Options(useGeneratedKeys)` 回填主键（原 `job.getId()` 插入后为 null 的隐患）
- **为什么改**：`NotificationService.sendNotification()` 原本存在但全项目无调用点，通知功能是空壳。
- **面试怎么讲**：功能闭环意识——"写好的能力必须被业务真正使用"；事务内通知保证一致性（同一事务，失败回滚）。
- **追问预案**：Q：通知和业务在同一事务，通知失败会回滚业务吗？A：会，这正是想要的强一致；如要解耦可用事务消息/发件箱模式（后续可演进）。

### P0-08【2026-08-27】文件上传白名单 + 目录穿越防护

- **模块**：`controller/FileController.java`
- **改动**：
  1. 上传后缀白名单（pdf/doc/docx/jpg/jpeg/png/gif/txt/md），拒绝可执行文件/脚本
  2. `GET /api/files/{dateDir}/{filename}`：目录必须是 8 位数字（yyyyMMdd）；文件名拒绝 `..`、`/`、`\`；`normalize()` 后校验仍在 uploadDir 根目录内
- **为什么改**：原接口直接拼路径读文件，`../` 可越出上传目录读取任意文件（目录穿越），且上传无类型限制。
- **面试怎么讲**：文件类接口的安全三件套——类型白名单、路径规范化校验、根目录边界检查。
- **追问预案**：Q：为什么不按 Content-Type 校验？A：Content-Type 可伪造，后缀白名单 + 文件名服务端重命名（UUID）双保险；更严格可再加文件头魔数校验。

### P0-09【2026-08-27】学生岗位列表服务端分页 + 搜索/筛选下沉 SQL

- **模块**：`mapper/JobMapper.java`、`service/JobService.java`、`controller/JobController.java`、`frontend/src/components/StudentJobs.vue`
- **改动**：
  1. `GET /student/jobs` 支持 `page`/`pageSize`/`keyword`/`departmentId` 参数，返回 `PageResult{records,total,page,pageSize}`
  2. 排序（本部门优先 + 薪资降序）、关键词搜索（标题/描述 LIKE）、部门筛选全部下沉到 SQL，配合 **PageHelper** 分页
  3. `JobService.getPublishedJobsPage`：`finally` 中 `PageHelper.clearPage()` 防御性清理 ThreadLocal
  4. 前端 `StudentJobs.vue`：列表改用分页接口 + `a-pagination` 分页器，搜索/筛选改为服务端查询（原为前端内存过滤，分页后会只过滤当前页）
- **为什么改**：原列表一次拉全量、前端内存过滤；数据量上来后不可持续，且分页后内存过滤语义错误。
- **面试怎么讲**：分页三要素——SQL 层排序保证分页语义正确、PageHelper 拦截器自动拼 LIMIT 与 count、`clearPage()` 防 ThreadLocal 泄漏；"排序/筛选/分页在数据库做"是性能基本盘。
- **追问预案**：Q：为什么不用前端分页？A：全量拉取随数据量增长不可扩展；服务端分页 + count 查询是标准做法。Q：PageHelper 原理？A：MyBatis 拦截器在执行前改写 SQL 追加 LIMIT，并执行 count 查询填充 total。

## P1-Spring AI Alibaba 接入（完成）

### P1-01【2026-08-30】Spring Boot 3.2.5 → 3.4.5 升级 + Spring AI Alibaba 1.0.0.2 接入（链路已真实验证）

- **模块**：`pom.xml`、`application-dev.yml`、`docker-compose.yml`、`config/SecurityConfig.java`、新增 `llm/` 包（ChatService / EmbeddingService / PromptTemplates）、`controller/LlmController.java`、`.gitignore`
- **改动**：
  1. **Spring Boot 3.2.5 → 3.4.5**：Spring AI Alibaba 1.0.0.2 官方对应 Spring AI 1.0.0 / Boot 3.4.x（[版本兼容表](http://java2ai.com/docs/1.0.0.2/faq/)）；Knife4j 4.4.0 → 4.5.0 适配
  2. 引入 `spring-ai-alibaba-starter-dashscope`（注意不是 `spring-ai-alibaba-starter`，BOM 内无此 artifact，踩坑记录）
  3. `llm` 包：`ChatService`（ChatModel 封装）、`EmbeddingService`（向量化）、`PromptTemplates`（P2 画像抽取/推荐精排/P4 审核 Prompt 集中管理）
  4. `LlmController`：`POST /api/llm/chat`（对话冒烟）、`POST /api/llm/embed`（向量冒烟）
  5. **JDBC URL 加 `allowPublicKeyRetrieval=true`**：Boot 3.4 自带 mysql-connector-j 9.1.0，MySQL 8 caching_sha2_password 认证必须该参数，否则 `Public Key Retrieval is not allowed`（升级踩坑）
  6. 加 actuator 健康检查（`/actuator/health` 匿名白名单）；新建 `.gitignore`（IDE/target/node_modules/uploads/密钥）
- **API Key 处理**：`application-dev.yml` 中 `spring.ai.dashscope.api-key: ${DASHSCOPE_API_KEY:sk-...}`（本地默认值），**生产/公开仓库务必改用环境变量注入**
- **验证（真实启动 + 真实调用 DashScope）**：
  - `mvn test` → 28 测试全绿
  - `POST /api/llm/chat`（admin 登录后）→ `code=200`，qwen-plus 真实回复
  - `POST /api/llm/embed` → `code=200`，向量维度 1024（text-embedding-v3）
  - 无 token 调 `/api/llm/chat` → HTTP 401（URL 级默认拒绝生效）
  - student1 调 `/api/admin/jobs/audit` → `code=403 没有操作权限`（AOP 角色校验生效）
- **面试怎么讲**：
  - 版本兼容性：Spring AI Alibaba 四位版本号，前三位对齐 Spring AI，BOM 统一管理
  - 框架价值：ChatModel/EmbeddingModel 自动配置，一行配置换模型（DashScope ↔ Ollama）
  - 分层设计：`llm` 包封装模型调用，`PromptTemplates` 集中 Prompt，业务代码不直接碰 AI SDK
  - 升级踩坑：mysql-connector-j 9.x 的 caching_sha2_password 认证变化（体现"升级不是无痛的"）
- **追问预案**：
  - Q：为什么升级 Boot？A：Spring AI Alibaba GA 只支持 Boot 3.4+；3.2 无法用 GA 版
  - Q：Key 泄露风险？A：环境变量注入 + .gitignore 排除 + 生产用阿里云密钥管理
  - Q：为什么用 DashScope 不用 OpenAI？A：国内直连稳定、中文效果好、阿里官方生态；Spring AI 抽象层保留切换能力

## 待办（后续阶段）

- [x] P0-01~P0-09 全部完成（2026-08-27，28 测试全绿）：
  安全加固（默认拒绝/角色鉴权/数据越权/文件安全/密码/日志）、状态机 bug、通知闭环、服务端分页
- [x] P1-01 Spring AI Alibaba 1.0.0.2 接入 + LLM/Embedding 链路真实验证（2026-08-30）
- [ ] P2 Agent 1 智能求职推荐（RAG：画像构建 + 岗位向量化 + 多路召回 + LLM 精排）—— 下一步
- [ ] P3 Agent 1 对话式求职助手（Function Calling + SSE）
- [ ] P4 Agent 2 智能审核（HITL）
